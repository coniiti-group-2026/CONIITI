apiVersion: v1
kind: ConfigMap
metadata:
  name: notifications-service-config
data:
  DB_HOST: "notifications-db-service"
  DB_PORT: "5432"
  DB_NAME: "notificationsdb"
  DB_USER: "admin"
  RABBITMQ_HOST: "rabbitmq-service"
---
apiVersion: v1
kind: Secret
metadata:
  name: notifications-service-secret
type: Opaque
stringData:
  DB_PASSWORD: "adminpassword"
  DATABASE_URL: "postgresql://admin:adminpassword@notifications-db-service:5432/notificationsdb"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notifications-service
  labels:
    app: notifications-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notifications-service
  template:
    metadata:
      labels:
        app: notifications-service
    spec:
      containers:
      - name: notifications-service
        image: notifications-service:latest
        command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "300m"
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: notifications-service-config
        - secretRef:
            name: notifications-service-secret
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: notifications-service
spec:
  type: ClusterIP
  selector:
    app: notifications-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
