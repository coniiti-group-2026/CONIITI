apiVersion: v1
kind: ConfigMap
metadata:
  name: users-service-config
data:
  DB_HOST: "users-db-service"
  DB_PORT: "5432"
  DB_NAME: "usersdb"
  DB_USER: "admin"
---
apiVersion: v1
kind: Secret
metadata:
  name: users-service-secret
type: Opaque
stringData:
  DB_PASSWORD: "adminpassword"
  DATABASE_URL: "postgresql://admin:adminpassword@users-db-service:5432/usersdb"
  INTERNAL_SERVICE_TOKEN: "coniiti-internal-secret-2026"
  JWT_SECRET_KEY: "change-this-in-production-please"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-service
  labels:
    app: users-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users-service
  template:
    metadata:
      labels:
        app: users-service
    spec:
      containers:
      - name: users-service
        image: users-service:latest
        command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "300m"
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: users-service-config
        - secretRef:
            name: users-service-secret
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: users-service
spec:
  type: ClusterIP
  selector:
    app: users-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
